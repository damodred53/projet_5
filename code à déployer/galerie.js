/*const allLink = document.querySelectorAll('.navigation-link')
let numberCategory;

allLink.forEach(function(link) {
    link.addEventListener('click', function(event){
        if(!link.classList.active)
            {
                let getLink = document.querySelectorAll('.navigation-link');
                getLink.forEach(function(element) {
                element.classList.remove('active')
                });
                
                link.classList.add('active');
                getPictures();
                return
            }
        
    })
})*/

/*const getPictures = () => {
let pictures = document.querySelectorAll('.gallery-item');
pictures.forEach(function(element) {
    
    let monImage = document.createElement('img');
    monImage.src= element.src;
    let maDiv = document.querySelector('.gallery').appendChild(monImage);

})
}*/

/*const getPictures = (event) => {
    if (event.target.id ===1)


}*/